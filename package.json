{
  "name": "claude-code",
  "version": "1.0.0",
  "private": true,
  "description": "Build an AI-powered coding agent — curriculum and tests for the Claude Code system",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "@types/node": "^22.0.0"
  }
}
