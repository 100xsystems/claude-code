{
  "name": "claude-code",
  "version": "1.0.0",
  "private": true,
  "description": "Build an AI-powered coding agent — curriculum and tests for the Claude Code system",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@100xsystems/test-suite-typescript": "^0.1.0",
    "vitest": "^3.0.0",
    "@types/node": "^22.0.0"
  }
}
